<web-app    xmlns="https://jakarta.ee/xml/ns/jakartaee"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
            version="6.0">

    <servlet>
        <servlet-name>facesServlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>facesServlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>

    <resource-ref>
        <description>DB Connection</description>
        <res-ref-name>jdbc/DiscordClone</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>
    <!-- TODO:
        Create a resource-ref of type:
            javax.sql.DataSource
        with auth set to:
            Container
        that references the name:
            jdbc/DiscordClone
        with a description of:
            DB Connection
    -->
    <!-- TODO:
        Add a resource for this DataSource to your Tomcat 10.x's context.xml.
        It should look something like:
            <Resource
                name="jdbc/DiscordClone"
                auth="Container"
                type="javax.sql.DataSource"
                maxTotal="100"
                maxIdle="30"
                maxWaitMillis="10000"
                username="root"
                password="root_password"
                driverClassName="org.mariadb.jdbc.Driver"
                url="jdbc:mariadb://localhost:3306/discord_clone"
                />

        Use the included db.sql to initialize your database.
    -->
</web-app>